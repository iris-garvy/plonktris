# bigger main-thread stack — the single-threaded prover recurses past the ~1MB wasm default
[target.wasm32-unknown-unknown]
rustflags = [
  "-C", "link-arg=-z",
  "-C", "link-arg=stack-size=33554432",
]