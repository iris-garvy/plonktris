[package]
name = "wasm"
version = "0.1.0"
edition = "2021"

[dependencies]
plonky2 = { version = "1.1", default-features = false, features = ["parallel"] }
circuit = { path = "../circuit" }
wasm-bindgen = "0.2"
rayon = "1.8"
wasm-bindgen-rayon = "1.2"

[lib]
crate-type = ["cdylib", "rlib"]