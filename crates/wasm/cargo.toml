[package]
name = "wasm"
version = "0.1.0"
edition = "2021"

[dependencies]
plonky2 = { version = "1.1", default-features = false }
circuit = { path = "../circuit" }
wasm-bindgen = "0.2"

[lib]
crate-type = ["cdylib", "rlib"]